schema: 'http://localhost:3000/graphql'
generates:
  ./src/shared/infrastructure/resolvers-types.ts:
    config:
      useIndexSignature: true
      contextType: 'src/shared/infrastructure/apollo-context#ApolloContext'
    plugins:
      - typescript
      - typescript-resolvers
